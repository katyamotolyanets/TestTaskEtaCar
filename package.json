{
  "scripts": {
    "frontend": "node --max_old_space_size=3072 && cd frontend && npm i --only=prod && npm start",
    "backend": "cd server && npm i --omit=dev && npm start",
    "start": "concurrently \"npm run backend\" \"npm run frontend\""
  },
  "dependencies": {
    "concurrently": "^7.5.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.4"
  }
}
