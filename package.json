{
  "scripts": {
    "frontend": "PORT=3000 cd frontend && npm i && npm start",
    "backend": "PORT=8080 cd server && npm i && npm start",
    "build": "cd server && npm i && tsc",
    "start": "concurrently \"npm run backend\" \"npm run frontend\""
  },
  "dependencies": {
    "concurrently": "^7.5.0",
    "express": "^4.18.2",
    "ts-node": "^10.9.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.8.4"
  }
}
